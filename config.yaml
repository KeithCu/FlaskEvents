# Flask Events Configuration

# Database Settings
database:
  path: "events.db"
  echo: false

# Flask Settings
secret_key: "your-secret-key-change-this-in-production"

# Admin login (run: python hash_password.py 'your-password')
admin:
  username: "admin"
  password_hash: ""

# Timezone Settings
timezone:
  local: "America/New_York"

